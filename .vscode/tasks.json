{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "shell",
			"label": "C++: Build current file with G++",
			"command": "g++",
			"args": [
				"-std=c++20",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}.exe",
				"-Wall",
				"-Wextra"
			],
			"group": "build",
			"problemMatcher": "$gcc",
			"detail": "Compile the currently open C++ file"
		},
		{
			"type": "shell",
			"label": "C++: Build project directory with G++",
			"command": "sh",
			"args": [
				"-c",
				"cd '${fileDirname}' && g++ -std=c++20 -Wall -Wextra *.cpp -o main"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$gcc",
			"detail": "Compile all C++ files in the current file's directory (project-aware)"
		},
		{
			"type": "cppbuild",
			"label": "C++:Build current file with Clang++",
			"command": "clang++",
			"args": [
				"-std=c++20",
				"${file}",
				"-o",
				"${fileDirname}/${fileBasenameNoExtension}.exe",
				"-Wall",
				"-Wextra"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$gcc",
			"detail": "Compile the currently open C++ file with Clang++"
		}
		{
			"type": "cppbuild",
			"label": "C++: Build project directory with Clang++",
			"command": "clang++",
			"args": [
				"-std=c++20",
				"${fileDirname}/*.cpp",
				"-o",
				"${fileDirname}/main"
			],
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"problemMatcher": "$gcc",
			"detail": "Compile all C++ files in the current file's directory (project-aware) with Clang++"
		}
	]
}